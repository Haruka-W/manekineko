<h1 class="subtitle is-1 has-text-primary">募集一覧ページ</h1>

<h5 class="subtitle is-5">投稿件数：全<%= @counts %>件</h5>
<%= render 'invites/index', invite: @invite %>

<%= paginate @invites, class: "paginate" %>
