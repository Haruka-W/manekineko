<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>招き猫</title>
    <!-- フォントオーサム導入 -->
    <script src="https://kit.fontawesome.com/3f94a5995c.js" crossorigin="anonymous"></script>
    <!-- BULMA導入 -->
    <link rel="stylesheet" href="https://jenil.github.io/bulmaswatch/minty/bulmaswatch.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- ヘッダーここから -->
    <header>
    <nav class="navbar has-background-primary" role="navigation" aria-label="main navigation">
        <div class="container">
            <div class="navbar-start">
              <%= link_to(image_tag("logo1.png"), root_path) %>
            </div>
            <!-- ログイン時 -->
            <% if user_signed_in? %>
              <div class="navbar-center navbar-item">
                <span class="has-text-white"><%= current_user.name %> さんでログイン中</span>
              </div>
              <div class="navbar-end navbar-item">
                <%= link_to 'マイページ', "#", class: 'button' %>
                <%= link_to '募集一覧', "#", class: 'button' %>
                <%= link_to '投稿一覧', "#", class: 'button' %>
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'button' %>
              </div>
            <!-- ログアウト時 -->
            <% else %>
              <div class="navbar-end navbar-item">
                <%= link_to '初めての方はこちら', homes_about_path, class: 'button' %>
                <%= link_to 'ログイン', new_user_session_path, class: 'button' %>
              </div>
            <% end %>
        </div>
    </nav>
</header>
    <!-- ヘッダーここまで -->

    <!-- メインここから -->
    <section class="section">
    <div class="container">
      <%= yield %>
    </div>
    </section>
    <!-- メインここまで -->

    <!-- フッターここから -->
    <footer class="footer has-background-primary">
      <!-- ここにimage_tagでロゴを入れる -->
      <p class="footer-text has-text-white has-text-centered">© 2020 haruka_watanabe</p>
    </footer>
    <!-- フッターここまで -->
  </body>
</html>
